@model myAbpBasic.Web.Models.Person.IndexViewModel

@{
    ViewBag.Title = L("PersonList");
    ViewBag.ActiveMenu = "PersonList"; //Matches with the menu name in SimpleTaskAppNavigationProvider to highlight the menu item
}
@*@section scripts
    {
        <environment names="Development">
            <script src="~/js/views/tasks/index.js"></script>
        </environment>

        <environment names="Staging,Production">
            <script src="~/js/views/tasks/index.min.js"></script>
        </environment>
    }*@
<h2>
    @L("PersonList")
    @*<span><a class="btn btn-primary btn-sm" asp-action="Create">@L("AddNew")</a></span>*@
    @*<span class="pull-right">
            @Html.DropDownListFor(
                model => model.SelectedTaskState,
                Model.GetTasksStateSelectListItems(LocalizationManager),
                new
                {
                    @class = "form-control",
                    id = "TaskStateCombobox"
                })
        </span>*@
</h2>
<div class="row">
    <div>
        <ul class="list-group" id="PersonList">
            @foreach (var person in Model.Persons)
            {
            <li class="list-group-item">
                @*<span class="pull-right label @Model.GetTaskLabel(task)">@L($"TaskState_{task.State}")</span>*@
                <h4 class="list-group-item-heading">@person.Name</h4>
                <div class="list-group-item-text">
                    @person.CreationTime.ToString("yyyy-MM-dd HH:mm:ss"))
                </div>
            </li>
            }
        </ul>
    </div>
</div>
